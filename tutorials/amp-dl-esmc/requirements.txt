# AMP + ESM-C 端到端教程依赖
# 建议在 conda 环境 amp-esm 中安装：
#   conda activate amp-esm
#   pip install -r requirements.txt

# 核心
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
pyyaml>=6.0
tqdm>=4.65
joblib>=1.3

# 生物序列
biopython>=1.81

# 深度学习（按你机器 CUDA 情况可改用 conda 装 torch）
torch>=2.1

# 蛋白语言模型：ESM-C / ESM3（EvolutionaryScale）
# 若 amp-esm 已装可跳过；冲突时见 README 的 fallback
esm>=3.0.0

# 可选：ESM-2 回退（fair-esm），当 esm 包不可用时
# fair-esm>=2.0.0

# 可视化 / 解释
matplotlib>=3.7
seaborn>=0.13
shap>=0.44

# 可选：加速注意力（Linux + 匹配 CUDA 时）
# flash-attn

# 完整依赖（请在 Python 3.10–3.12 的 conda 环境安装，例如 amp-esm / amp-esmc）
#   conda activate amp-esm   # 确认 which python 不含 medgeclaw-rnaseq
#   pip install -r requirements.txt

-r requirements-minimal.txt

tqdm>=4.65
biopython>=1.81
matplotlib>=3.7
seaborn>=0.13
shap>=0.44

# 深度学习 — 若 pip 失败请按 https://pytorch.org 官方命令装
torch>=2.1

# ESM-C / ESM3（EvolutionaryScale）。与 fair-esm 包名冲突，勿同环境混装
esm>=3.0.0
